# 뽀득뽀득 제품 정책

이 문서는 화면 구현 중 반복해서 참고할 제품 정책을 모아둔다.
첨부 PRD는 제품 방향의 근거로 참고하되, 실제 구현 범위는 사용자의 현재 요청과 MVP 상태를 우선한다.

## 홈 상태 표현 정책

홈의 상태 표현은 청결 점수나 실패 여부를 평가하지 않는다.
마지막 관리일과 권장 주기 대비 경과율을 기준으로, 사용자가 다음 행동을 가볍게 판단하도록 돕는다.

기본 기준:

- `Empty`: 청소 기록이 없는 상태. "오늘 한 곳만 칠해도 충분해요"처럼 첫 행동을 낮은 부담으로 안내한다.
- `Active Cleaning`: 짧은 시간 안에 여러 기록이 이어지는 상태. "오늘 벌써 n곳을 관리했어요"처럼 성취를 보여준다.
- `Maintained`: 최근 기록이 있고 권장 주기를 넘지 않은 상태. "잘 유지되고 있어요"처럼 색이 유지되는 느낌을 준다.
- `Needs Care`: 권장 주기에 가까워졌거나 지난 항목이 있는 상태. "슬슬 다시 볼 곳이 있어요"처럼 부드럽게 추천한다.

상태 산정 기준:

뽀득뽀득은 사용자에게 "청소율 70%"처럼 집의 청결도를 평가하는 숫자를 직접 보여주지 않는다.
대신 내부 계산에는 PRD의 `elapsedRatio`를 퍼센트로 바꾼 `관리 필요도 %`를 사용한다.

```text
elapsedRatio = 마지막 관리 이후 경과 시간 / 권장 관리 주기
관리 필요도 % = elapsedRatio * 100
```

상태 기준:

- `Empty`: 기록된 Item이 0개.
- `Active Cleaning`: 오늘 세션에서 2개 이상 Item을 연속 기록.
- `Maintained`: 기록된 Item이 있고, 모든 주요 Item의 관리 필요도 %가 85% 미만.
- `Needs Care`: 하나 이상의 주요 Item이 관리 필요도 85% 이상.

Item 표현 기준:

- 0% 이상 60% 미만: 최근 관리했어요.
- 60% 이상 85% 미만: 잘 유지되고 있어요.
- 85% 이상 110% 미만: 슬슬 다시 볼 때예요.
- 110% 이상: 한번 관리해볼까요?

최근 관리 흔적 문구 기준:

- 0% 이상 60% 미만: `어제 저녁 · 뽀득뽀득해요`
- 60% 이상 85% 미만: `n일 전 · 깨끗해요`
- 85% 이상 110% 미만: `n일 전 · 슬슬 청소해볼까요?`
- 110% 이상: `n일 전 · 청소가 필요해요`

MVP에서는 실제 날짜 계산 대신 mock item의 `elapsedRatio` 값을 사용한다.
기록 화면이나 추천 카드에서 Item을 기록하면 해당 Item/Space의 `elapsedRatio`를 0으로 간주한다.

사용하지 않는 표현:

- 미완료
- 실패
- 지연
- overdue
- 위험
- 더러움

## 홈 이모티콘 정책

이모티콘은 상태를 빠르게 감지하게 하는 보조 신호로만 사용한다.
버튼, 기록 완료 여부, 탭 이동 같은 명령 아이콘을 대체하지 않는다.

상태별 기본 이모티콘:

- `Empty`: 🙂  
  첫 기록 전의 부담 없는 시작 상태.
- `Active Cleaning`: 😄  
  오늘 여러 항목을 연속으로 기록 중인 상태.
- `Maintained`: 😊  
  최근 관리 색이 아직 유지되는 상태.
- `Needs Care`: 🧽  
  다시 볼 곳이 있지만 실패나 경고는 아닌 상태.

운영 원칙:

- 한 화면에서 이모티콘은 핵심 상태 카드 중심으로 제한한다.
- 체크 표시, 반짝이, 경고성 기호를 반복적으로 쓰지 않는다.
- 상태가 바뀌어도 사용자를 재촉하거나 혼내는 인상을 주지 않는다.
- 컬러는 공간별 파스텔과 충돌하지 않도록 작은 배경 위에 제한적으로 사용한다.

## 색상과 Fade 정책

청소 기록은 체크리스트 완료 표시보다 색칠된 상태로 표현한다.
기록 직후에는 해당 공간의 파스텔 컬러가 선명하게 보이고, 시간이 지나면 같은 계열 안에서 천천히 옅어진다.

원칙:

- 공간별 색상은 캔버스와 기록 카드에서 일관되게 사용한다.
- 완료 직후와 권장 주기 도달 전 상태는 서로 다른 색으로 보이지 않게 한다.
- 권장 주기에 가까워질수록 색을 옅게 만들되 완전히 사라지게 하지 않는다.
- 전체 앱 배경은 흰색과 뉴트럴을 유지하고, 공간 컬러는 캔버스/선택 상태에 집중해서 사용한다.

## 대청소 기록 정책

대청소 모드는 여러 공간을 훑는 화면이지만, 전체 완료를 요구하지 않는다.
사용자가 체크한 Item만 CleaningLog 후보로 취급한다.
일반 기록 화면이 방금 한 청소를 빠르게 남기는 곳이라면, 대청소 화면은 오래 안 한 항목과 숨은 관리 항목을 공간별로 훑는 곳이다.

종료 정책:

- 대청소 종료 역할을 하는 버튼은 하나만 둔다.
- 기본 종료 버튼은 `n건 · 오늘의 대청소 종료`로 표현한다.
- 선택한 항목이 없으면 "기록 없이 마치기"로 표현한다.
- "전체 선택"은 종료가 아니라 선택 보조 액션이다.
- 전체 선택 후에도 사용자가 종료 버튼을 누르기 전까지 기록된 것으로 보지 않는다.

선택 표현:

- 체크 버튼은 항목 선택 상태를 보여주는 명령 UI로 사용한다.
- 선택된 항목 카드는 해당 공간의 파스텔 컬러로 채운다.
- 선택 직후에는 카드 안에 "이제 깨끗해요!" 피드백을 짧게 보여준다.
- 체크되지 않은 항목은 미완료로 표현하지 않는다.
- 선택된 항목에서는 마지막 기록, 숨은 관리, 팁 문구를 숨기고 카드 채움만 남긴다.

구성 원칙:

- 화면 상단에는 대청소의 목적과 먼저 볼 항목 순서를 보여준다.
- 항목은 최근 청소하지 않은 것과 숨은 관리 항목을 우선 배치한다.
- 공간별 아코디언을 유지해 한 번에 보이는 목록의 부담을 줄인다.
- "숨은 관리"는 작은 보조 문구로만 표시하고 과한 뱃지로 만들지 않는다.
- 기록이 없는 일반 항목은 보조 기록 문구를 비워둔다.
- 기록이 없는 숨은 관리 항목은 `숨은 관리 · 놓치기 쉬워요`로 표시한다.
- 기록이 있는 숨은 관리 항목은 `숨은 관리 · n일 전`으로 표시한다.
- 각 항목에는 한 줄 팁을 둘 수 있다. 단, 선택 완료 상태에서는 팁을 숨긴다.
- 대청소 종료 버튼에는 "저장"이라는 말을 쓰지 않는다. `n건 · 오늘의 대청소 종료`처럼 오늘 세션 종료로 표현한다.
- 대청소를 종료하면 선택한 항목은 Record의 최근 기록에 우선 노출한다.

## 체크 버튼 사용 정책

체크 버튼은 사용자가 추천 후보나 대청소 후보 중 기록할 항목을 고르는 화면에서만 사용한다.

- `Record`: 카드 전체를 누르는 원터치 기록 화면이므로 별도 체크 버튼을 두지 않는다.
- `Home 추천`: 추천 후보를 바로 기록하는 영역이므로 체크 버튼을 둔다.
- `Deep Clean`: 여러 후보 중 일부를 고르는 화면이므로 체크 버튼을 둔다.

체크 버튼이 있는 경우에도 완료 피드백의 중심은 버튼이 아니라 카드의 공간별 파스텔 채움과 "이제 깨끗해요!" 메시지다.

## 헤더와 뱃지 정책

홈을 제외한 주요 화면은 `뽀득뽀득 > 현재 화면` 구조로 현재 위치를 보여준다.
브랜드 로고와 `뽀득뽀득` 영역은 홈 이동 역할을 한다.

뱃지 사용은 최소화한다.

- 날짜, 마지막 기록, 권장 주기, 상태 문구는 기본적으로 제목 아래 보조 텍스트로 표시한다.
- 사용자가 누를 수 있는 것처럼 보이는 알약형 뱃지는 상태 정보에 쓰지 않는다.
- 뱃지는 필터, 선택 모드, 아주 짧은 카운트처럼 조작과 직접 관련된 곳에만 제한적으로 사용한다.
