# 뽀득뽀득 제품 정책

이 문서는 화면 구현 중 반복해서 참고할 제품 정책을 모아둔다.
첨부 PRD는 제품 방향의 근거로 참고하되, 실제 구현 범위는 사용자의 현재 요청과 MVP 상태를 우선한다.

아래에서 **제품 원칙**은 앞으로 유지할 방향이고, **현재 프로토타입**은 지금 화면에서 확인되는 동작이다. 시연용 문구·날짜·잔액을 실제 계정 데이터나 완성된 기능으로 해석하지 않는다.

## 시작과 기본 공간 정책

- 첫 화면의 `게스트로 시작하기`는 회원가입 없이 공간 선택 화면으로 이동한다. 별도의 1초 채색 체험 화면도 존재하지만 현재 첫 진입 흐름에는 연결되어 있지 않다.
- 공간 선택 화면은 기본 공간 4개를 모두 선택한 상태로 시작한다. 사용자는 일부를 해제할 수 있지만 하나도 선택하지 않으면 시작 버튼을 누를 수 없다.
- 완료 시 선택한 공간과 기본 방 이름 `방`을 브라우저에 보관하고 홈으로 이동한다. 홈·공간·기록·대청소는 이 선택을 반영한다. 설정 전 직접 해당 화면에 접근하면 목업 공간이 보일 수 있다.
- `침실 / 방`은 선택 화면에서 `방`으로 표시한다. 현재 화면에는 방 이름을 바꾸는 입력 기능이 없다.
- 기본 공간 외 확장 버튼은 가격과 가입 시 시작 포인트 혜택을 설명하는 안내창만 연다. 안내창을 닫고 기본 공간으로 계속할 수 있으며 이 단계에서 공간은 추가되지 않는다.

`/sample-paint`의 카드 채색 체험과 `/home/:state`의 홈 상태 예시는 직접 주소로 볼 수 있는 시연 화면이다. 현재 시작·기록 흐름의 상태나 실제 기록 데이터에 반영되지 않는다.

## 기본 공간과 포인트 확장 정책

핵심 청소 기록 경험은 무료로 제공한다. 사용자는 회원가입이나 포인트 없이 기본 공간을 선택하고 청소를 기록할 수 있다.

- 기본 제공 공간은 `거실`, `주방`, `욕실`, `방`의 4개다. 필요한 공간만 선택해서 시작할 수 있다.
- 시작 화면에서는 기본 공간을 `방`으로 표시한다. 이름 변경은 추후 제공할 무료 설정으로 분류하고 공간 추가로 계산하지 않는다.
- 기본 공간의 청소 항목, 기록, 최근 기록 확인은 포인트가 없어도 사용할 수 있다.
- 기본 공간 외에 공간을 1개 추가하는 확장 가격은 `300P`다. 집 크기나 주거 형태 자체를 유료 조건으로 잠그지 않는다.
- 포인트는 청소 활동 보상이 아니라 유료 확장에 쓰는 결제 재화다. 회원가입 시 제공할 시작 포인트 `300P`는 첫 공간 확장을 체험하기 위한 일회성 혜택으로 구분한다.
- 포인트와 가격은 첫 진입의 주 CTA보다 낮은 위계로 보여준다. `공간 추가 · 300P`를 누르면 가격과 시작 포인트 혜택을 설명하고, 기본 공간으로 계속할 수 있게 한다.
- 청소 기록을 하려면 포인트가 필요하다는 인상을 주지 않는다. 홈의 주요 행동은 관리 추천과 기록에 집중한다.

현재 프로토타입은 설정과 포인트 화면에 체험용 보유 포인트 `500P`를 표시한다. 유효한 별도 시연 잔액이 브라우저에 있으면 그 값을 읽는다. 포인트 화면은 `보유 포인트 → 포인트로 구매하기 → 포인트 충전하기` 순서이며 사용처는 공간 1개 추가(`300P`)만 보여준다. 충전 묶음은 `300P`, `500P`, `1,000P`이며 기본 선택은 `300P`다. 원화 가격은 아직 표시하지 않는다. 홈·공간 화면의 공간 추가 진입점에서는 잔액이 충분하면 상품 영역, 부족하면 충전 영역을 목표로 한다. 목표 영역이 현재 화면에 모두 보이면 스크롤하지 않고, 가려져 있을 때만 필요한 만큼 이동한다. 헤더의 포인트 아이콘으로 직접 들어오면 상단 보유 포인트부터 보여준다. 잔액 부족 상태에서 공간 추가 버튼은 충전 영역으로 이동한다. 그 밖의 구매 버튼은 준비 중 토스트만 표시하며 잔액이나 공간을 변경하지 않는다. 실제 결제, 원화 판매 가격, 공간 구매 로직과 광고 제거 상품 정책은 추후 확정·구현한다.

## 홈 상태 표현 정책

홈의 상태 표현은 청결 점수나 실패 여부를 평가하지 않는다.
마지막 관리일과 권장 주기 대비 경과율을 기준으로, 사용자가 다음 행동을 가볍게 판단하도록 돕는다.

제품 설계 기준(현재 홈 화면의 자동 계산과는 구분):

- `Empty`: 청소 기록이 없는 상태. "오늘 한 곳만 칠해도 충분해요"처럼 첫 행동을 낮은 부담으로 안내한다.
- `Active Cleaning`: 짧은 시간 안에 여러 기록이 이어지는 상태. "오늘 벌써 n곳을 관리했어요"처럼 성취를 보여준다.
- `Maintained`: 최근 기록이 있고 권장 주기를 넘지 않은 상태. "잘 유지되고 있어요"처럼 색이 유지되는 느낌을 준다.
- `Needs Care`: 권장 주기에 가까워졌거나 지난 항목이 있는 상태. "슬슬 다시 볼 곳이 있어요"처럼 부드럽게 추천한다.

향후 상태 산정 기준:

뽀득뽀득은 사용자에게 "청소율 70%"처럼 집의 청결도를 평가하는 숫자를 직접 보여주지 않는다.
대신 내부 계산에는 PRD의 `elapsedRatio`를 퍼센트로 바꾼 `관리 필요도 %`를 사용한다.

```text
elapsedRatio = 마지막 관리 이후 경과 시간 / 권장 관리 주기
관리 필요도 % = elapsedRatio * 100
```

상태 기준:

- `Empty`: 기록된 Item이 0개.
- `Active Cleaning`: 오늘 세션에서 2개 이상 Item을 연속 기록.
- `Maintained`: 기록된 Item이 있고, 모든 주요 Item의 관리 필요도 %가 85% 미만.
- `Needs Care`: 하나 이상의 주요 Item이 관리 필요도 85% 이상.

Item 표현 기준:

- 0% 이상 60% 미만: 최근 관리했어요.
- 60% 이상 85% 미만: 잘 유지되고 있어요.
- 85% 이상 110% 미만: 슬슬 다시 볼 때예요.
- 110% 이상: 한번 관리해볼까요?

최근 관리 흔적 문구 기준:

- 0% 이상 60% 미만: `어제 저녁 · 뽀득뽀득해요`
- 60% 이상 85% 미만: `n일 전 · 깨끗해요`
- 85% 이상 110% 미만: `n일 전 · 슬슬 청소해볼까요?`
- 110% 이상: `n일 전 · 청소가 필요해요`

현재 프로토타입은 실제 날짜나 Item별 권장 주기로 위 상태를 재계산하지 않는다. 홈 추천 후보의 목업 `elapsedRatio`는 우선 추천 대상을 고를 때만 사용한다. 홈 상태 카드는 목업 관리 항목 수와 세션 기록 수, 남은 추천 후보에 따라 `최근 기록이 없는 곳이 있어요`·`오늘 n곳을 관리했어요`·`[항목] 청소는 어때요?`·`최근 자주 관리한 곳이에요` 중 하나를 보여준다. 현재 이모티콘은 순서대로 🫧·🙂·🧽·🙂이다. 아래의 상태별 이모티콘은 제품 방향으로 남겨둔 기준이다.

홈은 `상태 카드 → 공간별 청소 캔버스 → 최근 관리 흔적 → 추천 후보 → 대청소 진입` 순서다. 공간 카드를 누르면 해당 공간의 기록 화면으로, 최근 관리 흔적을 누르면 항목 히스토리로 이동한다. 최근 흔적은 최대 4개를 먼저 보여주고 더 많으면 히스토리로 연결한다. 추천 후보와 상태 카드는 해당 공간의 기록 화면으로 이동하며 홈에서 즉시 기록하지 않는다. 대청소에서 종료한 항목은 같은 브라우저 탭의 최근 흔적과 기록 화면의 최근 목록에 우선 노출된다. 일반 기록 화면에서 채색한 항목은 현재 홈의 추천 완료·최근 흔적·캔버스 수치와 연동되지 않는다.

사용하지 않는 표현:

- 미완료
- 실패
- 지연
- overdue
- 위험
- 더러움

## 홈 이모티콘 정책

이모티콘은 상태를 빠르게 감지하게 하는 보조 신호로만 사용한다.
버튼, 기록 완료 여부, 탭 이동 같은 명령 아이콘을 대체하지 않는다.

제품 설계상 상태별 기본 이모티콘(현재 홈 구현과 다를 수 있음):

- `Empty`: 🙂  
  첫 기록 전의 부담 없는 시작 상태.
- `Active Cleaning`: 😄  
  오늘 여러 항목을 연속으로 기록 중인 상태.
- `Maintained`: 😊  
  최근 관리 색이 아직 유지되는 상태.
- `Needs Care`: 🧽  
  다시 볼 곳이 있지만 실패나 경고는 아닌 상태.

운영 원칙:

- 한 화면에서 이모티콘은 핵심 상태 카드 중심으로 제한한다.
- 체크 표시, 반짝이, 경고성 기호를 반복적으로 쓰지 않는다.
- 상태가 바뀌어도 사용자를 재촉하거나 혼내는 인상을 주지 않는다.
- 컬러는 공간별 파스텔과 충돌하지 않도록 작은 배경 위에 제한적으로 사용한다.

## 색상과 Fade 정책

청소 기록은 체크리스트 완료 표시보다 색칠된 상태로 표현한다.
제품 방향상 기록 직후에는 해당 공간의 파스텔 컬러가 선명하게 보이고, 시간이 지나면 같은 계열 안에서 천천히 옅어진다.

원칙:

- 공간별 색상은 캔버스와 기록 카드에서 일관되게 사용한다.
- 완료 직후와 권장 주기 도달 전 상태는 서로 다른 색으로 보이지 않게 한다.
- 권장 주기에 가까워질수록 색을 옅게 만들되 완전히 사라지게 하지 않는다.
- 전체 앱 배경은 흰색과 뉴트럴을 유지하고, 공간 컬러는 캔버스/선택 상태에 집중해서 사용한다.

현재 홈 캔버스의 색 농도는 공간별 목업 `관리 중인 항목 수 / 전체 항목 수`를 네 단계의 투명도로 바꿔 표현한다. 실제 시간 경과에 따른 Fade는 아직 동작하지 않는다. 대청소에서 기록한 공간은 세션 중 캔버스 수치가 한 단계 보정되지만, 일반 기록 화면의 채색과는 연결되지 않는다.

## 대청소 기록 정책

대청소 모드는 여러 공간을 훑는 화면이지만, 전체 완료를 요구하지 않는다.
사용자가 체크한 Item만 CleaningLog 후보로 취급한다.
일반 기록 화면이 방금 한 청소를 빠르게 남기는 곳이라면, 대청소 화면은 오래 안 한 항목과 숨은 관리 항목을 공간별로 훑는 곳이다.

종료 정책:

- 대청소 종료 역할을 하는 버튼은 하나만 둔다.
- 기본 종료 버튼은 `n건 · 오늘의 대청소 종료`로 표현한다.
- 선택한 항목이 없으면 현재 화면에서는 `오늘의 대청소 종료`로 표현한다. 이때 기록 없이 홈으로 돌아간다.
- "전체 선택"은 종료가 아니라 선택 보조 액션이다.
- 전체 선택 후에도 사용자가 종료 버튼을 누르기 전까지 기록된 것으로 보지 않는다.

선택 표현:

- 체크 버튼은 항목 선택 상태를 보여주는 명령 UI로 사용한다.
- 선택된 항목 카드는 해당 공간의 파스텔 컬러로 채운다.
- 선택 직후에는 카드 안에 "이제 깨끗해요!" 피드백을 짧게 보여준다.
- 체크되지 않은 항목은 미완료로 표현하지 않는다.
- 선택된 항목에서는 마지막 기록, 숨은 관리, 팁 문구를 숨기고 카드 채움만 남긴다.

구성 원칙:

- 화면 상단에는 대청소의 목적과 먼저 볼 항목 순서를 보여준다.
- 항목은 최근 청소하지 않은 것과 숨은 관리 항목을 우선 배치한다.
- 공간별 아코디언을 유지해 한 번에 보이는 목록의 부담을 줄인다.
- "숨은 관리"는 작은 보조 문구로만 표시하고 과한 뱃지로 만들지 않는다.
- 기록이 없는 일반 항목은 보조 기록 문구를 비워둔다.
- 기록이 없는 숨은 관리 항목은 `숨은 관리 · 놓치기 쉬워요`로 표시한다.
- 기록이 있는 숨은 관리 항목은 `숨은 관리 · n일 전`으로 표시한다.
- 각 항목에는 한 줄 팁을 둘 수 있다. 단, 선택 완료 상태에서는 팁을 숨긴다.
- 대청소 종료 버튼에는 "저장"이라는 말을 쓰지 않는다. `n건 · 오늘의 대청소 종료`처럼 오늘 세션 종료로 표현한다.
- 대청소를 종료하면 선택한 항목은 Record의 최근 기록에 우선 노출한다.

현재 화면은 선택한 기본 공간만 표시하고, 미리 지정한 우선순위에 따라 공간과 항목을 정렬한다. 상단에는 먼저 볼 공간 최대 3개와 숨은 관리·다시 볼 항목 수를 보여준다. 첫 공간만 펼친 상태로 시작하며 공간별 아코디언을 열고 닫을 수 있다. `전체 선택`은 접힌 공간을 포함해 현재 대상 공간의 모든 항목을 고르고, 모두 고른 상태에서는 `전체 선택 해제`로 바뀐다. 종료 시 선택한 항목을 브라우저 탭의 세션 데이터에 반영하고 홈으로 이동한다. 과거의 대청소 최근 목록은 새 종료 결과로 교체된다.

## 일반 기록과 공간 정책

- 기록 화면은 `최근 기록`·`자주`·`공간별 보기` 필터를 제공하고, 기본 진입은 `공간별 보기`다. 공간별 보기에서는 설정 때 선택한 공간만 탭에 보인다. 홈의 공간·추천, 가이드의 기록 CTA에서 진입하면 해당 공간을 먼저 보여준다.
- 일반 기록은 카드 전체를 한 번 누르면 공간 색으로 채우고 약 1.2초 동안 `이제 깨끗해요!`를 보여준다. 같은 카드를 다시 누르면 채색을 해제한다. 별도의 저장 버튼은 없다. 최근·자주 목록에서도 카드를 누르는 방식은 같다.
- 채색한 카드 ID는 같은 브라우저 탭의 세션에만 남는다. 최근 청소 날짜 문구는 목업이며, 채색 후 실제 시각으로 갱신되지 않는다. 최근·자주·공간별 목록은 일부 이름과 ID가 서로 다른 목업 데이터여서 하나의 정규화된 항목 목록으로 취급하지 않는다.
- 공간 화면은 `전체 보기`와 선택된 기본 공간 탭으로 분류한다. 각 공간의 항목은 최근 관리 문구와 `관리 중`·`슬슬 확인`·`확인 필요`·`아직 기록 없음` 상태를 보여주며, 누르면 항목 상세로 이동한다. 이 상태와 권장 주기는 현재 고정 목업 데이터다. `항목 추가` 진입점과 공간 확장 진입점도 표시한다.
- 항목 추가·상세 화면에서는 이름, 공간, 1일 이상 청소주기를 입력하거나 바꿀 수 있다. 현재 `저장`은 짧은 확인 문구만 보여주고 공간 목록, 기록 카드 또는 다른 화면의 데이터는 변경하지 않는다. 항목을 실제로 추가·수정하는 정책과 저장 기능은 미구현이다.
- 공간 상세 화면은 공간 상태, 권장 주기, 최근 기록의 별도 목업을 보여준다. 공간 목록의 상태나 실제 세션 기록과 자동 동기화하지 않는다.

## 히스토리와 청소가이드 정책

- 히스토리는 월별 목업 기록을 `일자별 기록`·`공간별 기록`·`자주 돌본 곳`으로 볼 수 있다. 이전·다음 달 이동, 오늘로 이동, 공간별 펼치기가 가능하다. 현재 화면의 `오늘`과 기록 기준일은 시연용 `2026-09-04`로 고정되어 있다. 빈 달에는 기록 화면으로 가는 안내를 보여준다.
- 항목 히스토리는 해당 목업 항목의 이번 주·이번 달·올해·전체 횟수와 일별 목록·월별 달력을 보여준다. 기록 화면의 채색 및 대청소 세션 결과는 현재 히스토리 집계에 추가되지 않는다.
- 청소가이드는 `놓치기 쉬운 관리`와 공간별 `기본 가이드`를 제공한다. 가이드 상세는 관리 방법 단계, 함께 보면 좋은 항목, 최근 기록 문구를 보여주고 `청소 기록하러 가기`로 해당 공간의 기록 화면에 연결한다. 현재 가이드와 관련 항목은 목업 데이터이며, 관련 항목 카드가 항상 해당 항목 상세로 이동하는 것은 아니다.
- 권장 주기와 상태는 청소를 강제하거나 실패를 판단하기 위한 표현이 아니다. 가이드의 주기 안내와 공간·히스토리의 목업 수치는 아직 하나의 실시간 계산 결과가 아니다.

## 체크 버튼 사용 정책

체크 버튼은 여러 대청소 후보 중 기록할 항목을 고르는 화면에서 사용한다.

- `Record`: 카드 전체를 누르는 원터치 기록 화면이므로 별도 체크 버튼을 두지 않는다.
- `Home 추천`: 현재 후보 카드를 누르면 해당 공간의 기록 화면으로 이동한다. 홈 카드에는 체크 버튼을 두지 않는다.
- `Deep Clean`: 여러 후보 중 일부를 고르는 화면이므로 체크 버튼을 둔다.

체크 버튼이 있는 경우에도 완료 피드백의 중심은 버튼이 아니라 카드의 공간별 파스텔 채움과 "이제 깨끗해요!" 메시지다.

## 헤더와 뱃지 정책

홈 헤더는 브랜드명과 로고를 보여준다. 다른 주요 화면의 헤더는 로고와 현재 화면 제목을 한 줄로 보여주며, 해당 영역을 누르면 홈으로 이동한다. 현재 구현은 `뽀득뽀득 > 현재 화면` 형태의 브레드크럼이 아니다.
헤더의 포인트 아이콘은 포인트 화면, 설정 아이콘은 설정 화면으로 이동한다. 알림 아이콘은 표시만 하며 실제 알림은 제공하지 않는다. 하단 탐색은 `홈`·`공간`·`기록`·`히스토리`·`청소가이드`의 5개 탭이다. 대청소 화면은 청소가이드 탭을 활성 표시한다.

설정 화면은 보유 포인트와 `300P` 기준으로 추가 가능한 공간 수를 안내하고 포인트 화면으로 이동할 수 있게 한다. 추천·알림 항목의 스위치 모양과 도움말·개인정보 처리방침·버전 항목은 현재 표시용이며 설정 변경이나 별도 화면으로 연결되지 않는다.

뱃지 사용은 최소화한다.

- 날짜, 마지막 기록, 권장 주기, 상태 문구는 기본적으로 제목 아래 보조 텍스트로 표시한다.
- 사용자가 누를 수 있는 것처럼 보이는 알약형 뱃지는 상태 정보에 쓰지 않는다.
- 뱃지는 필터, 선택 모드, 아주 짧은 카운트처럼 조작과 직접 관련된 곳에만 제한적으로 사용한다.

현재 공간 목록의 상태, 가이드의 권장 주기, 공간 상세의 주기는 알약형으로 표시되어 위 제품 원칙과 다르다. 이 화면들의 상태 표현은 아직 정리되지 않은 목업으로 보며, 새로운 화면에 그대로 확장하지 않는다.
