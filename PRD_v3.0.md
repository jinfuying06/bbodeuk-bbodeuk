# 뽀득뽀득 PRD v3.0 — 통합판

**색칠하며 챙기는 집 청소 · 2030 1인 가구를 위한 청소기록 앱** *(기준일: 2026-09-17 · PRD v2.5 + PRODUCT\_POLICY.md 통합, AI 온보딩(AX) 챕터 신규 추가)*

---

## v3.0에서 달라진 점

1. **문서 일원화** — 그동안 `PRODUCT_POLICY.md`에만 있던 세부 정책(경과율 임계값, 색상/Fade 규칙, 대청소 종료 절차, 헤더·뱃지·체크버튼 규칙 등)을 이 PRD 본문에 전부 병합했다. `PRODUCT_POLICY.md`는 화면 구현 중 빠르게 참고하는 세부 레퍼런스로 계속 쓰되, 제품 방향에 대한 최종 판단 기준은 이 문서 하나로 통일한다.
2. **AI 기반 공간/아이템 자동 설정(AX) 챕터 신규 추가** — SECTION 19. 사진으로 공간을 촬영하면 AI가 공간 유형과 대표 아이템을 인식해 Setup 온보딩을 대신 채워주는 기능이다. 현재는 설계 확정 단계이며, 실제 구현 시점과 우선순위는 리소스 상황에 따라 P1 이후로 조정될 수 있다.
3. **문서 성격 전환** — v2.4\~v2.5의 "SLIDE" 표기를 "SECTION"으로 바꾸고, 발표용 슬라이드 문서가 아니라 상시 참고하는 통합 레퍼런스 PRD로 재정의했다.
4. **현재 단계 명시** — 이 프로젝트는 검증되지 않은 개인 프로젝트/Usability Test 프로토타입 단계임을 문서 전반에 명확히 했다. 수익화 확장 아이디어(제휴 마케팅, 브랜드 협찬 등)는 지금 구현 범위와 섞이지 않도록 별도 로드맵 섹션(SECTION 22)으로 분리했다.

---

## 1. 네이밍 & USP

**제품명:** 뽀득뽀득 **부제:** 뽀득뽀득 : 색칠하며 챙기는 집 청소 **태그라인:** 한 곳만 칠해도, 오늘 청소는 성공.

**한 줄 정의:** 집의 현재 관리 상태를 한눈에 확인하고, 청소한 곳을 가볍게 기록하면 다음에 돌볼 곳을 자연스럽게 알려주는 청소 기록 앱.

**USP:** 해야 할 청소를 쌓는 대신, 이미 해낸 청소를 색으로 쌓는다.

## 2. 제품 개요

청소를 할 일 목록이 아니라 '관리되고 있는 집'으로 보여준다.

사용자가 세면대·변기·싱크대처럼 청소한 Item을 기록하면 해당 영역이 즉시 색으로 채워진다. 청소 기록은 체크리스트가 아니라 색칠된 집과 공간의 모습으로 남는다. 시간이 지나면 Item과 Space의 색이 자연스럽게 옅어지고, 앱은 마지막 관리 기록과 권장 주기를 비교해 슬슬 다시 볼 곳을 추천한다.

**제품 구조:** Home → Space → Item → Care Action

- Home: 집 전체와 현재 관리 상태
- Space: 욕실 · 주방 · 거실 · 방(침실) 등
- Item: 세면대 · 변기 · 싱크대 등 실제 청소 기록 대상
- Care Action: 수전 물때 제거 · 세면볼 닦기 · 배수구 관리 등 세부 안내

**핵심 경험:** See → Act → Record → Reward → Fade → Recommend → Return

## 3. 사용자 문제

사용자는 청소뿐 아니라 '청소를 기억하고 관리하는 일'에도 피로를 느낀다.

1. 작은 청소의 성취가 보이지 않는다
2. 마지막 관리일과 다음 시점을 기억하기 어렵다
3. 보이지 않는 관리 항목을 놓친다
4. Todo 방식이 또 하나의 일이 된다
5. 기록 과정이 길면 사용을 포기한다

## 4. 핵심 사용자 페르소나

**이서윤 · 여성 · 29세** · 브랜드 마케팅 대행사 AE 4년 차 / 서울 오피스텔 1.5룸 / 자취 3년 차

> "집은 꾸준히 관리하고 싶지만, 뭘 언제 해야 하는지까지 일일이 기억하고 계획하긴 싫어요."

**UX 키워드:** Effortless · Lightweight · Seamless · Rewarding

## 5. 포지셔닝

Cleaning Tracker에서 시작해 Home-care System으로 확장한다.

**MVP Positioning — Cleaning-first** 공간 기반 청소 기록 / 마지막 관리 시점 확인 / Fade를 통한 상태 인지 / 작은 행동의 시각적 성취 / 다음 관리 대상 추천

**Long-term Positioning — Personal Home-care** 세탁조 관리 · 침구 관리 · 필터 교체 · 냉장고 관리 · 계절성 관리 · Custom Home-care Item

**초기 확장 범위에서 제외:** 장보기 · 일반 빨래 Todo · 설거지 Todo · 심부름 · 생활 전체 습관 관리

**수익 모델 방향(요약, 상세는 SECTION 22):** v2.4의 구독(Free/Plus) 가설을 폐기하고, 기본 공간 무료 + 추가 공간당 포인트 구매라는 트랜잭션형 모델을 1차 가설로 채택한다. 구독형은 장기 옵션으로 유보한다.

## 6. 핵심 UX 전략

상태 확인 → 작은 행동 → 기록 → 성취 → Fade → 추천 → 재관리

**핵심 차별화**

- Task-first → Achievement & State-first
- Penalty-based progress → Non-punitive maintenance

## 7. 정보구조(IA) & Bottom Navigation

**IA:** Home → Space → Item → Care Action

- Item = 기록 단위
- Care Action = 안내 단위 (Care Action을 매번 체크하게 하지 않는다)

**Bottom Navigation:** 홈 | 공간 | 기록 | 히스토리 | 청소가이드

- 홈: 현재 관리 상태(색의 짙음으로 표현), 다시 볼 곳 확인, 최근 청소 내역 확인, 대청소모드 진입
- 공간: 공간별 전체 상태 탐색, 최근청소내역·자주찾은 청소내역 확인
- 기록: 가운데 강조된 빠른 기록 Action, 원터치로 기록 및 해제
- 히스토리: 날짜별 CleaningLog와 누적 기록, 일자별(월간 달력)/공간별 확인
- 청소가이드: 관리 Guide / Discovery / 권장 주기

**구현 현황(정책):** 헤더는 홈에서는 브랜드명+로고, 다른 화면에서는 로고+현재 화면 제목을 한 줄로 보여주고 해당 영역을 누르면 홈으로 이동한다. 브레드크럼 형태는 아니다. 헤더의 포인트 아이콘은 포인트 화면, 설정 아이콘은 설정 화면으로 이동한다. 알림 아이콘은 표시만 하며 실제 알림은 없다. 대청소 화면은 청소가이드 탭을 활성 표시한다.

## 8. Home 설계 — 역할/상태/우선순위

**Home = Status + Navigation + Achievement**

Above the Fold: Contextual Hero / 추천 관리 대상 / 관리 상태 핵심 요약 / Home Canvas 일부 Below the Fold: 전체 Home Canvas / 최근 관리 / 기록 없는 곳 / Discovery / Deep Clean 진입

**화면 우선순위:** 1) Contextual Hero → 2) 추천 관리 대상 → 3) Home Canvas → 4) 최근 관리 → 5) 기록 없는 곳/Discovery → 6) Deep Clean. 최근 관리 흔적은 최대 4개까지만 먼저 보여주고 초과분은 히스토리로 연결한다.

**홈 상태 4종**

- STATE A · Empty Home — 기록된 Item 0개. "오늘 청소한 곳 하나만 기록해도 괜찮아요." → CTA: 첫 청소 기록하기
- STATE B · Active Cleaning — 오늘 세션에서 2개 이상 연속 기록. "오늘 벌써 n곳을 관리했어요." → CTA: 계속 기록하기 / Secondary: 오늘은 여기까지
- STATE C · Maintained Home — 기록이 있고 모든 주요 Item이 임계치 미만. "잘 유지되고 있어요."
- STATE D · Needs Care — 하나 이상의 주요 Item이 임계치 이상. "슬슬 다시 볼 곳이 있어요."

**이모티콘(정책):** Empty 🙂 · Active Cleaning 😄 · Maintained 😊 · Needs Care 🧽. 상태를 빠르게 감지하는 보조 신호로만 쓰고 버튼·완료여부·탭 이동 같은 명령 아이콘을 대체하지 않는다. 상태가 바뀌어도 재촉하거나 혼내는 인상을 주지 않는다.

**홈 화면 순서(정책):** 상태 카드 → 공간별 청소 캔버스 → 최근 관리 흔적 → 추천 후보 → 대청소 진입. 추천 후보·상태 카드는 해당 공간 기록 화면으로 이동하며 홈에서 즉시 기록하지 않는다.

**구현 현황:** 실제 홈 로직은 "기록된 항목 없음 → 오늘 세션 2건 이상 → 활성 추천 존재 → 그 외" 우선순위로 4가지 문구·이모티콘을 계산한다(`getHomeStatus()`, 현재 이모티콘 순서 🫧·🙂·🧽·🙂). 세션 기록 수와 추천 후보가 서로 다른 목업 소스에서 와서 통합이 필요하다(SECTION 25 참고).

## 9. 관리 상태 & Fade 모델

청결도를 평가하지 않는다. 관리 경과 상태를 보여준다.

**공통 계산 기준(목표 모델)**

```
elapsedRatio = 마지막 관리 이후 경과 시간 / 권장 관리 주기
관리 필요도 % = elapsedRatio × 100
```

**UI 해석 구간(확정치)**

- 0%\~60%: 최근 관리했어요
- 60%\~85%: 잘 유지되고 있어요
- 85%\~110%: 슬슬 다시 볼 때예요
- 110%+: 한번 관리해볼까요?

**홈 상태 산정 기준(정책):** Empty(기록 0개) / Active Cleaning(오늘 세션 2개 이상 연속 기록) / Maintained(기록 있고 모든 주요 Item 관리 필요도 85% 미만) / Needs Care(하나 이상 85% 이상).

**최근 관리 흔적 문구(정책):**

- 0\~60%: 어제 저녁 · 뽀득뽀득해요
- 60\~85%: n일 전 · 깨끗해요
- 85\~110%: n일 전 · 슬슬 청소해볼까요?
- 110%+: n일 전 · 청소가 필요해요

**Fade(색상 정책):** elapsedRatio가 증가할수록 색이 연속적으로 옅어진다. 색은 완전히 사라지지 않는다. 공간별 색상은 캔버스와 기록 카드에서 일관되게 사용하고, 앱 전체 배경은 흰색/뉴트럴을 유지한다.

> 시간은 성취를 지우지 않고, 다시 볼 시점을 만든다.

**사용하지 않는 표현:** 미완료 · 실패 · 지연 · overdue · 위험 · 더러움

**구현 현황:** 현재는 화면별 목업 수치를 쓰고 있어 같은 항목이 화면마다 다른 근거로 보일 수 있다. 홈 캔버스 색 농도는 공간별 목업 비율을 4단계 투명도로 표현하며, 실제 시간 경과에 따른 Fade는 아직 동작하지 않는다. "Phase 1(현재, 화면별 목업)"과 "Phase 2(목표, 단일 계산 엔진)"로 로드맵을 분리한다.

## 10. Recommended Care Engine & 기본 권장주기

마지막 관리일과 권장 주기를 기반으로 다음 관리 대상을 자동 추천한다.

**기본 데이터(목표 모델)**

```
Item: name, defaultInterval, category, space, importance, hiddenCareFlag
```

**추천 계산(목표 모델)**

```
activeInterval = customIntervalDays ?? defaultIntervalDays
elapsedRatio = (now - lastCleanedAt) / activeInterval
```

**사용자 표현 / 금지 표현:** "최근 관리했어요" / "슬슬 다시 볼 때예요" / "한번 관리해볼까요?" / "아직 기록이 없어요" 사용, overdue·지연·미완료·실패·위험 금지.

**기본값 원칙:** 기본값은 앱이 제공하고, 사용자는 필요할 때만 바꾼다.

**구현 현황:** 추천/상태/Fade는 목표 모델에서는 같은 elapsedRatio를 공유하지만 현재는 화면별로 분리되어 있다.

## 11. Item Library 전략

최대한 많은 관리 항목을 지원하되 처음부터 전부 보여주지 않는다.

Core Items / Hidden·Important Care Items 구분은 청소가이드 탭에서 "놓치기 쉬운 관리"와 공간별 "기본 가이드"로 구현된다.

**Custom Item:** 이름·Space·권장 주기·메모를 입력하는 화면은 있으나 현재 실제로 목록에 추가되지 않는다(P0.5 미구현).

**Product Principle:** Coverage는 넓게, 기본 노출은 가볍게.

## 12. Spaces 화면

Home이 요약이라면 Spaces는 전체 탐색이다.

전체 보기와 사용자가 설정 단계에서 고른 기본 공간 탭으로 구성한다. 각 Item은 관리 중·슬슬 확인·확인 필요·아직 기록 없음 상태와 함께 표시되며 누르면 항목 상세로 이동한다. 항목 추가 진입점과 공간 확장 진입점도 표시한다.

**구현 현황:** 이 상태와 권장 주기는 현재 고정 목업이며, 공간 목록의 상태나 실제 세션 기록과 자동 동기화되지 않는다.

## 13. Record 화면

방금 한 청소를 최소한의 동작으로 기록한다.

**구성:** 자주 기록 / 최근 기록 / 공간별(설정에서 고른 공간만 탭 노출). 기본 진입은 공간별 보기다.

**인터랙션:** 카드 전체 한 번 탭 = 즉시 채색 + 저장. 별도 저장 버튼 없음.

- 탭 한 번으로 공간 색으로 채워지고 약 1.2초간 "이제 깨끗해요!" 피드백을 보여준다.
- 같은 카드를 다시 탭하면 채색을 해제한다(취소 가능한 토글).
- Batch Paint는 여러 카드를 각각 탭하는 방식으로 대체되어 있다(별도 확인 절차 없음).

**자동 저장(목표 모델)**

```
CleaningLog: itemId, cleanedAt, createdAt, source
```

**구현 현황:** 채색한 카드 ID는 같은 브라우저 탭의 세션에만 남는다. 최근 청소 날짜 문구는 목업이며 채색 후 실제 시각으로 갱신되지 않는다. 최근·자주·공간별 목록은 일부 이름·ID가 서로 다른 목업 데이터라 하나의 정규화된 항목 목록으로 취급하지 않는다.

## 14. Deep Clean / 대청소 모드

평소 기록과 대규모 청소 경험을 분리한다. 여러 공간을 훑는 화면이지만 전체 완료를 요구하지 않는다. 사용자가 체크한 Item만 CleaningLog 후보로 취급한다.

**핵심 원칙:** 전체 완료를 요구하지 않는다 / 체크되지 않은 항목은 미완료로 표현하지 않는다 / 진행률 %를 강조하지 않는다 / 1개만 체크해도 정상 완료다.

**종료 정책:** 종료 버튼은 하나만 둔다. 기본은 `n건 · 오늘의 대청소 종료`, 선택 항목이 0개면 `오늘의 대청소 종료`(이때 기록 없이 홈으로 복귀). `전체 선택`은 종료가 아니라 선택 보조 액션이며 접힌 공간을 포함한 모든 항목을 고르고, 모두 고른 뒤엔 `전체 선택 해제`로 바뀐다. 종료 버튼 문구에 "저장"을 쓰지 않는다.

**선택 표현(정책):** 체크 버튼은 항목 선택 상태를 보여주는 명령 UI. 선택된 카드는 공간 파스텔 컬러로 채우고 "이제 깨끗해요!"를 짧게 보여준다. 선택 완료 상태에서는 마지막 기록·숨은 관리·팁 문구를 숨기고 카드 채움만 남긴다.

**구성 원칙(정책):** 상단에 목적과 먼저 볼 항목 순서 표시 / 최근 청소하지 않은 것과 숨은 관리 항목 우선 배치 / 공간별 아코디언 유지 / "숨은 관리"는 작은 보조 문구로만(과한 뱃지 금지) / 기록 없는 숨은 관리 항목은 `숨은 관리 · 놓치기 쉬워요`, 기록 있으면 `숨은 관리 · n일 전` / 항목별 한 줄 팁 가능(선택 완료 시 숨김).

**Deep Clean Reward 예시:** "오늘 6곳을 관리했어요." (욕실 3 · 주방 2 · 거실 1) — 보여주지 않는 것: 6/18 완료 · 33% · 남은 12개. "오늘은 이 정도면 충분해요."

**Deep Clean과 기존 기록의 관계(목표 원칙):** 기존 기록을 덮어쓰지 않는다. CleaningLog는 누적되고, 현재 상태는 가장 최근 기록을 기준으로 계산한다.

**구현 현황:** 위 규칙 대부분이 실제 화면(`WeekendBigClean.tsx`)에 반영되어 정합도가 가장 높은 기능이다. 다만 종료 결과는 세션에만 저장되고, 과거 대청소 최근 목록은 새 종료 결과로 교체된다(누적이 아님). 현재 화면은 선택한 기본 공간만 표시하고, 상단에 먼저 볼 공간 최대 3개와 숨은 관리·다시 볼 항목 수를 보여주며, 첫 공간만 펼친 채 시작한다.

## 15. Item Detail & Care Action / Soft Cleaning Session

**Item Detail:** 마지막 기록/현재 상태/기본 권장 주기/사용자 설정 주기/최근 CleaningLog + Care Action 안내로 구성된다. Care Action은 참고 정보이며, 기본 기록은 여전히 세면대 Paint 하나로 끝난다.

**구현 현황:** 화면 구조는 구현되어 있으나 상태·주기 값이 별도 목업이라 공간 목록·실제 세션 기록과 자동 동기화되지 않는다.

**Soft Cleaning Session:** 사용자가 Session을 관리하지 않는다. Item Paint는 즉시 CleaningLog(현재는 세션 기록)로 저장되고, 연속 기록은 필요에 따라 UI에서만 묶는다. 선택적 종료: "오늘은 여기까지". 자동 종료: 일정 시간 추가 기록이 없으면 Active Cleaning 상태 종료.

## 16. 핵심 경험 루프

See → Act → Record → Reward → Fade → Recommend → Return

## 17. 온보딩 & First Success

첫 성공까지 60초 이내. 로그인/게스트 갈림길에 더해, v3.0은 AI 사진인식 기반 공간 설정을 세 번째 선택 경로로 추가한다(상세는 SECTION 19).

**경로 A · 게스트**

1. Login 화면에서 "로그인 없이 둘러보기"
2. 공간 선택(Setup) — 기본 4개 공간이 모두 선택된 채 시작, 하나 이상 유지해야 진행 가능
3. 확장 배너("다른 공간도 관리하고 싶나요? · 300P")는 안내창만 열고 기본 공간으로 계속 진행 가능
4. Empty Home → 5. First Paint(Quick Record 카드 탭 한 번) → 6. Reward("이제 깨끗해요!" + 홈 상태 갱신)

**경로 B · 로그인/회원가입**

1. 앱 실행 → Login이 루트(`/`) 화면
2. 이메일 로그인 또는 카카오/구글 소셜 버튼(현재 UI만, 인증 로직 없음) → 제출 시 홈으로 이동
3. 또는 "이메일로 회원가입" → 이메일/비밀번호/닉네임 입력 + 약관 동의(필수 2, 선택 1) → 제출 시 Setup으로 이동
4. 이후 경로 A의 2\~6과 동일

**경로 C · AI 사진 설정(신규, 선택적)** — SECTION 19에서 상세 정의. "빠르게 시작하기"(기본 4개 공간 바로 사용)와 "내 공간 사진으로 맞춤 설정하기"를 Setup 진입 시 분기해, 사진 온보딩이 First Paint 목표(60초 이내)를 늦추지 않도록 한다.

**핵심 원칙**

- 일정·반복 주기 입력 필수 아님
- 기본 권장 주기를 앱이 제공
- 공간 선택만으로 시작 가능
- Custom 주기는 나중에 설정
- 로그인 여부는 First Paint 경험의 속도나 단계 수를 바꾸지 않는다
- (신규) AI 사진 설정 경로를 고르지 않아도 First Paint까지의 단계 수는 늘어나지 않는다

**구현 갭 / 정리 필요**

- ✅ 반영 완료: Setup의 뒤로가기 대상을 `/login`으로 통일. `Welcome.tsx`/`/welcome` 라우트는 직접 주소로는 진입 가능한 상태로 남아 있어, 별도 랜딩이 필요 없다고 확정되면 제거한다.
- ✅ 반영 완료: `HashRouter`(`/#/login`)에서 `BrowserRouter`(`/login`)로 전환, GitHub Pages용 `404.html` SPA 폴백(`spa-github-pages` 방식) 추가.
- 로그인/회원가입 제출은 실제 인증 없이 항상 성공 처리된다(폼 검증만 존재). MVP 범위에서는 의도된 상태이나 백엔드 연동 전까지 "회원 데이터 보존" 약속은 하지 않는다.

**시작과 기본 공간 정책(정책 병합):** 게스트로 시작하기는 회원가입 없이 공간 선택 화면으로 이동한다. 별도의 1초 채색 체험 화면(`/sample-paint`)과 홈 상태 예시(`/home/:state`)도 존재하지만 직접 주소로만 볼 수 있는 시연 화면이며 현재 시작·기록 흐름에는 연결되지 않는다. 공간 선택 화면은 기본 4개를 모두 선택한 상태로 시작하고 하나 이상 유지해야 진행 가능하다. 완료 시 선택한 공간과 기본 방 이름 `방`을 브라우저에 보관하고 홈으로 이동한다. `침실/방`은 `방`으로 표시하며 현재 이름 변경 입력 기능은 없다. 기본 공간 외 확장 버튼은 가격과 시작 포인트 혜택을 설명하는 안내창만 열고, 안내창을 닫으면 공간은 추가되지 않는다.

## 18. 인증 & 계정 정책

계정은 선택, 첫 기록은 항상 무장벽.

**진입점:** 앱 최초 진입(`/`) = Login 화면. 이메일 로그인 / 카카오 로그인 / Google 로그인(버튼만, 미연동) / 이메일 회원가입 / 게스트로 둘러보기.

**회원가입 수집 정보:** 이메일, 비밀번호(8자 이상), 닉네임, 약관 동의(서비스 이용약관·개인정보 수집 필수, 마케팅 수신 선택).

**원칙**

- 로그인/회원가입은 데이터를 기기 간에 보존하고 싶은 사용자를 위한 옵션이지 필수 관문이 아니다.
- 게스트로 시작한 사용자도 나중에 회원 전환 시 로컬 기록을 이어받을 수 있어야 한다(현재 미구현 — 로컬/서버 데이터 병합 정책은 P1에서 정의).
- 소셜 로그인 버튼은 브랜드 가이드라인을 따르는 아이콘·색상을 그대로 쓰되, 실제 OAuth 연동 전까지는 "준비 중" 처리로 폴백한다.

**MVP 범위:** P0(로그인/회원가입/게스트 화면 UI, 폼 유효성 검사, 약관 동의 게이팅) → P1(실제 인증, 세션 유지, 게스트→회원 데이터 이관) → P2(다중 기기 동기화, 계정 삭제/탈퇴, 소셜 계정 연결 관리).

## 19. AI 기반 공간/아이템 자동 설정(AX) — 신규 챕터

### 배경과 목적

Setup 온보딩에서 사용자가 공간을 선택한 뒤 아이템을 하나씩 골라 담는 과정의 마찰을 줄이기 위해, 공간 사진을 찍으면 AI가 공간 유형과 대표 아이템을 인식해 초기 구성을 대신 채워주는 기능을 검토했다. 현재는 설계 확정 단계이며, 실제 구현 여부와 우선순위는 P1 이후 리소스 상황에 따라 조정한다. 개인 프로젝트/Usability Test 단계라는 현재 범위를 고려해, 이 챕터의 수익화 관련 아이디어는 SECTION 22로 분리했다.

### 플로우(제품 설계)

1. **사진 촬영** — 카메라를 기본 경로로 유도하되 앱범 선택을 막지 않는다(강제하지 않음). 공간당 1장을 기본으로 하고, "더 찍기"로 2\~3장까지 선택적으로 추가할 수 있다.
2. **공간/아이템 인식** — 비전 지원 AI에 구조화된 출력(JSON)으로 요청해 공간 유형과 눈에 보이는 대표 아이템을 한 번의 호출로 함께 받는다. 아이템은 자유 생성이 아니라 기존 Item Library taxonomy 안에서 분류하도록 제한한다.
3. **결과 확인(편집 가능한 초안)** — "이렇게 구성했어요, 확인해 주세요" 형태로 제시하고, 삭제·추가·수정 후 진행 또는 그대로 진행을 선택하게 한다. AI 결과는 어떤 경우에도 자동 확정되지 않는다.
4. **아이템 개수 한도** — 공간당 기본 아이템 슬롯(예: 10개, 정확한 수치는 P1에서 확정)을 두고, 그 안에서는 AI가 준 아이템을 자유롭게 삭제·재추가할 수 있어 AI 오인식이 곳바로 비용 부담으로 이어지지 않는다. 슬롯 자체를 늘리는 것을 유료 확장 대상으로 삼을지는 아래 "정책 검토 필요" 항목 참고.
5. **청소가이드 자동 매칭** — 확정된 아이템별로 정적 청소가이드 콘텐츠를 매칭해 청소가이드 탭을 미리 채운다(AI 실시간 생성 아님, 상세는 아래).
6. **앱 사용법 온보딩(스킵 가능)** → 메인 화면 진입.

### 설계 원칙(신규 제품 원칙 — SECTION 29에 번호 편입)

- AI 인식 결과는 확정이 아니라 항상 편집 가능한 초안이다. 자동으로 저장·확정하지 않는다.
- 청소 방법 가이드는 AI가 매번 새로 생성하지 않고, 사람이 검수한 정적 콘텐츠에서 아이템 매칭으로 제공한다.
- AI 아이템 인식은 자유 생성이 아니라 기존 Item Library taxonomy 내 분류로 제한한다.
- 공간 판별이 애매한 경우(복합 공간 등, 원룸/오피스텔 타겟 특성상 흔함) 단정적으로 거부하지 않고 후보를 제시해 사용자가 고르게 한다.
- 인식 실패가 반복되면(예: 2회) 수동 설정으로 자연스럽게 폴백한다. 무한 재촬영 루프를 만들지 않는다.
- 신뢰는 인식 정확도 자체보다 "틀렸을 때 수정 마찰이 얼마나 낮은가"로 확보한다.

### 청소가이드 콘텐츠 — 정적 DB 매칭(AI 실시간 생성 아님)

아이템별 하우투 가이드(예: 욕조 청소 — 뜨거운 물로 때를 불린다 → 세제를 뿌려둔다 → 솔로 닦는다)는 아이템 taxonomy가 고정되어 있으므로, 매번 AI가 실시간 생성하지 않고 사전에 작성·검수한 정적 콘텐츠 라이브러리에서 아이템 매칭으로 제공한다. 이유는 두 가지다: (1) 잘못된 청소 방법(예: 세제 혼합으로 인한 유독가스 발생, 재질 손상)은 안전 문제로 이어질 수 있어 AI 즉석 생성의 리스크가 브랜드 톤 문제보다 크다. (2) 정적 매칭은 추가 AI 비용이 들지 않는다. 초안 작성 시 AI를 보조 도구로 쓰더라도 최종 콘텐츠는 사람이 검수한다.

### 비용 메모(참고용, 확정 견적 아님)

사진 인식은 가벼운 비전 모델 1콜로 처리하면 사진 1장당 실비용이 사실상 무시 가능한 수준이다. 이 기능은 QuickRecord처럼 매일 반복되는 액션이 아니라 온보딩·공간 추가 시점에만 발생하는 저빈도 액션이라, 비용이 DAU가 아니라 신규 유저 수와 가끔의 재설정 횟수에만 비례한다. 청소가이드는 정적 매칭이라 추가 비용이 없다. 개인 프로젝트 단계에서는 비용이 실질적 병목이 아니다.

### 정책 검토가 필요한 지점

- **아이템 슬롯 한도의 유료화 여부** — 현재 제품 설계 원칙 15("포인트는 청소 행동이 아니라 공간 확장에만 관여한다")는 공간 단위 확장만을 유료 대상으로 규정한다. 아이템 개수를 유료 확장 대상에 포함시키는 것은 이 원칙의 적용 범위를 넘히는 결정이라, 도입 전 별도로 의사결정하고 문서에 명시해야 한다. 도입한다면 슬롯 한도를 실제 사용자군(원룸/오피스텔 1\~2인 가구)의 실제 아이템 수보다 넘무니 교하게 잡고, AI 초기 제안 개수보다 한도를 여유 있게 설정해 첫 온보딩 경험에서는 절대 걸리지 않게 한다.
- **준비물/제휴 마케팅 확장은 이 챕터의 범위에서 제외** — 청소가이드에 "필요한 준비물"을 붙이고 쿠파파트너스 등 제휴 링크나 브랜드 협찬을 연결하는 아이디어는 SECTION 22(수익 모델 — 향후 로드맵)로 분리했다. 지금 범위에는 포함하지 않는다.

### 리스크(SECTION 28에 항목 편입)

- 사진 인식 오탐지 자체보다, 오탐지 시 수정 마찰이 큰 것이 진짜 리스크다. 아이템 편집 UI를 최우선으로 매끄럽게 만든다.
- 온보딩 단계 증가로 First Paint까지의 시간이 늘어날 수 있다 → "빠르게 시작하기" 경로를 항상 병행 제공한다.
- 아이템 슬롯 유료화를 성급히 도입하면 "핵심 기록은 포인트로 막지 않는다"는 원칙과 충돌할 수 있다.

### MVP 범위 구분

- **P1(향후 검토):** 사진 기반 공간/아이템 인식 온보딩(대안 경로), 아이템별 정적 청소가이드 콘텐츠 라이브러리 구축.
- **P2 이후:** 준비물 추천 및 제휴/광고 링크, 브랜드 협찬 슬롯(SECTION 22 참고).

### KPI(SECTION 27에 항목 편입)

- 사진 온보딩 경로 선택률(빠른 시작 대비)
- AI 인식 결과 수정률(신뢰도 프록시 지표 — 낮다고 반드시 좋은 것은 아니며, 수정 자체가 쉬운지와 함께 해석)
- 사진 경로 이용자의 Setup 완료율·First Paint 도달 시간(빠른 시작 경로 대비 지연 여부 확인)

## 20. 청소가이드 & 히스토리 정책

**히스토리:** 월별 목업 기록을 `일자별 기록`·`공간별 기록`·`자주 돌본 곳`으로 볼 수 있다. 이전·다음 달 이동, 오늘로 이동, 공간별 펼치기가 가능하다. 빈 달에는 기록 화면으로 가는 안내를 보여준다.

**항목 히스토리:** 해당 항목의 이번 주·이번 달·올해·전체 횟수와 일별 목록·월별 달력을 보여준다.

**청소가이드:** `놓치기 쉬운 관리`와 공간별 `기본 가이드`를 제공한다. 가이드 상세는 관리 방법 단계, 함께 보면 좋은 항목, 최근 기록 문구를 보여주고 `청소 기록하러 가기`로 해당 공간 기록 화면에 연결한다.

권장 주기와 상태는 청소를 강제하거나 실패를 판단하기 위한 표현이 아니다.

**구현 현황:** 현재 `오늘`과 기록 기준일은 시연용 `2026-09-04`로 고정되어 있다. 기록 화면의 채색 및 대청소 세션 결과는 현재 히스토리 집계에 추가되지 않는다. 현재 가이드와 관련 항목은 목업 데이터이며, 관련 항목 카드가 항상 해당 항목 상세로 이동하는 것은 아니다. 가이드의 주기 안내와 공간·히스토리의 목업 수치는 아직 하나의 실시간 계산 결과가 아니다.

## 21. UI 정책 — 헤더/뱃지/체크버튼/이모티콘/색상

**체크 버튼 사용 정책:** 체크 버튼은 여러 대청소 후보 중 기록할 항목을 고르는 화면에서 사용한다. Record는 카드 전체를 누르는 원터치 기록 화면이므로 체크 버튼을 두지 않는다. Home 추천 카드도 체크 버튼 없이 누르면 해당 공간 기록 화면으로 이동한다. Deep Clean은 여러 후보 중 일부를 고르는 화면이므로 체크 버튼을 둔다. 체크 버튼이 있어도 완료 피드백의 중심은 버튼이 아니라 카드의 공간별 파스텔 채움과 "이제 깨끗해요!" 메시지다.

**뱃지 정책:** 뱃지 사용은 최소화한다. 날짜, 마지막 기록, 권장 주기, 상태 문구는 기본적으로 제목 아래 보조 텍스트로 표시한다. 사용자가 누를 수 있는 것처럼 보이는 알약형 뱃지는 상태 정보에 쓰지 않는다. 뱃지는 필터·선택 모드·아주 짧은 카운트처럼 조작과 직접 관련된 곳에만 제한적으로 사용한다.

**구현 현황:** 현재 공간 목록의 상태, 가이드의 권장 주기, 공간 상세의 주기는 알약형으로 표시되어 위 원칙과 다르다. 이 화면들의 상태 표현은 아직 정리되지 않은 목업으로 보며, 새로운 화면에 그대로 확장하지 않는다.

**설정 화면:** 보유 포인트와 `300P` 기준 추가 가능한 공간 수를 안내하고 포인트 화면으로 이동할 수 있게 한다. 추천·알림 항목의 스위치, 도움말·개인정보 처리방침·버전 항목은 현재 표시용이며 설정 변경이나 별도 화면으로 연결되지 않는다.

## 22. 수익 모델 — 포인트 및 향후 로드맵

v2.4의 Free/Plus 구독 모델을 폐기하고 포인트 기반 공간 확장 모델로 전환한다. 핵심 기록 경험(공간 선택, 청소 기록, Fade, 기본 추천, 기본 History, 기본 Care Guide)은 회원가입이나 포인트 없이 전부 무료다.

**무료로 제공:** 기본 공간 4개(거실·주방·욕실·방) / Quick Record 무제한 기록 / 기본 Fade·Recommended Care·History·Care Guide / 방 이름 변경(향후 무료 설정, 공간 추가로 계산하지 않음).

**포인트로 구매:** 기본 공간 외 공간 1개 추가 = 300P. 집 크기·거주 형태 자체를 유료 조건으로 잠그지 않는다 — 잠기는 것은 "공간 개수"뿐이다.

**포인트 정책:** 포인트는 청소 활동 보상이 아니라 유료 확장에 쓰는 결제 재화다(청소 기록에 대한 리워드 지급 없음). 회원가입 시 시작 포인트 300P를 1회 지급(첫 공간 확장 체험용). 충전 묶음: 300P/500P/1,000P(기본 선택 300P). 원화 가격 미정. 포인트/가격 UI는 첫 진입 주 CTA보다 낮은 위계로 노출해 청소 기록에 포인트가 필요하다는 인상을 주지 않는다.

**Monetization Hypothesis:** 사용자는 "구독해서 더 많은 기능을 여는 것"보다 "필요한 순간에 내 집 크기만큼만 결제해서 공간을 넘히는 것"에 더 거부감이 적은가? 구독형 Advanced Report·개인화 추천 같은 "해석 가치" 상품은 P2 이후 별도 검증한다.

**구현 현황:** 포인트 잔액 조회, 공간 확장 구매 진입, 충전 화면은 존재하나 실제 결제는 연결되어 있지 않다(구매 버튼은 "준비 중" 토스트). 잔액 부족 시 공간 추가 버튼은 충전 섹션으로 스크롤 이동하고, 그 밖 구매 버튼은 잔액·공간을 변경하지 않는다. 설정·포인트 화면은 체험용 보유 포인트 500P를 표시한다(별도 시연 잔액이 있으면 그 값을 읽음). 포인트 화면 순서는 `보유 포인트 → 포인트로 구매하기 → 포인트 충전하기`이며 사용처는 공간 1개 추가(300P)만 보여준다.

### 향후 로드맵 — AI 온보딩(SECTION 19) 연계 수익화 아이디어(개인 프로젝트 단계 범위 밖)

청소가이드에 "필요한 준비물"(예: 고무장갑·수세미·세제)을 붙이고, 아이템 단위로 다음 세 가지 관계 유형을 독립적으로 지정할 수 있는 구조를 검토했다: (1) 관계 없음 — 제네릭 이미지, 뱃지 없음, (2) 브랜드 협찬 — "광고" 뱃지 + 해당 브랜드 이미지, (3) 개인 제휴 링크(예: 쿠파파트너스) — "제휴" 뱃지 + 해당 이미지. 데이터 구조는 `아이템 → 정적 가이드 → 준비물 카테고리 → 관계유형/링크`로 분리해 나중에 링크·브랜드만 교체 가능하게 설계한다.

법적 고지 원칙(검토됨): 쿠파파트너스 등 제휴 링크는 약관상 "수수료를 받을 수 있다"는 문구를 그 노출 위치 바로 옷에 명시해야 하며, 이는 운영 주체가 개인인지 법인인지와 무관하게 경제적 대가관계가 있으면 적용된다. "광고"(브랜드 협찬)와 "제휴"(수수료형 링크) 뱃지는 서로 다른 대가관계를 나타내므로 구분해서 표시하고, 두 뱃지의 시각적 가시성은 동일하게 유지한다(어느 한쪽을 은근히 덜 보이게 하지 않는다). 초반에는 리스트 내 유료/제휴 항목 비율에 상한을 두어 전체가 광고판처럼 보이지 않게 한다.

이 로드맵은 실사용자 트래픽이 검증된 이후 단계의 검토 대상이며, 현재 MVP·P1 범위에는 포함하지 않는다.

## 23. 핵심 기능 맵 — P0/P0.5/P1/P2

**P0 · MVP**

1. Contextual Home
2. Home Care Status(4상태: Empty/Active Cleaning/Maintained/Needs Care)
3. Recommended Care Engine(현재는 화면별 목업, Phase 1)
4. Quick Record(Single Paint, 탭 = 즉시 기록)
5. CleaningLog(현재는 세션 범위 목업, Phase 1)
6. Spaces
7. History
8. Item Detail
9. 기본 Care Action Guide
10. 기본 Item Library(Core/Hidden 구분)
11. 기본 권장 주기(표시만, 저장 로직 없음)
12. Deep Clean Mode
13. 로그인/회원가입/게스트 진입 UI
14. 포인트 조회·공간 확장 구매 UI, 시작 포인트 지급 표시

**P0.5 · 다음 스프린트 최우선**

- CleaningLog 영구 저장소로 이관(탭/새로고침 생존)
- Home·Spaces·ItemInfo·History가 하나의 Item 상태표를 공유하도록 통합
- Custom Interval / Custom Item 실제 저장 연결
- 실제 이메일/소셜 인증 연동
- 포인트 구매·충전 실제 결제 연동, 원화 가격 확정

**P1**

- Discovery
- Contextual Advice
- Soft Reminder(알림 On/Off, 현재는 표시만 되는 토글)
- 게스트 → 회원 데이터 이관
- (신규) AI 기반 공간/아이템 자동 설정 온보딩 경로(SECTION 19)
- (신규) 아이템별 정적 청소가이드 콘텐츠 라이브러리

**P2**

- Multi Home
- Advanced Report
- Widget
- Theme / Palette
- Photo Log
- 고급 개인화 추천
- 광고 제거 등 추가 상품
- (신규) 준비물 추천 및 제휴/광고 링크(SECTION 22)

## 24. MVP 구현 범위

**Home:** Contextual Hero 4상태 / 추천 관리 Item / Home Canvas(커버리지 채도) / 최근 관리 / 기록 없는 곳 **Spaces:** 설정에서 고른 기본 공간 중심 / Space별 Item 목록 / 상태·마지막 관리·추천(목업) **Record:** 자주 기록 / 최근 기록 / 공간별 선택 / 탭 = 즉시 기록(별도 저장 버튼 없음) **Recommended Care Engine:** Phase 1(화면별 고정 목업 수치) / Phase 2(P0.5, `lastCleanedAt` 비교 기반 실제 elapsedRatio) **History:** 월별 보기(일자별/공간별/자주 돌본 곳), 현재는 시연용 고정 날짜(`2026-09-04`) 기준 목업 **청소가이드:** 기본 Guide / 숨은 중요 Item 일부 / 권장 주기 정보(표시만) **인증 & 포인트:** Login/SignUp UI, 게스트 경로, 포인트 잔액·구매·충전 화면(결제 미연동)

**MVP에서 만들지 않는 것:** 실제 평면도 편집기 / 청결도 점수 / overdue dashboard / 일반 Todo / 강제 Session / LLM 기반 추천 / Multi Home / 실제 결제·원화 가격 / 실제 인증 서버 연동 / 탭·기기 간 지속되는 CleaningLog / (신규) 실시간 AI 사진 인식(P1 검토 전까지는 목업으로 대체 가능) / (신규) 제휴·광고 링크 연동

## 25. 상태 및 데이터 모델

**HomeState:** `EmptyHome` / `ActiveCleaning` / `MaintainedHome` / `NeedsCareHome` **DisplayState:** `NeverCleaned` / `Maintained` / `NeedsAttention`

**Item(목표 모델)**

```
Item: id, name, spaceId, defaultIntervalDays, customIntervalDays?, importance, hiddenCareFlag
```

**CleaningLog(목표 모델)**

```
CleaningLog: id, itemId, cleanedAt, createdAt, source(single|batch|deepClean|backdated)
```

**Recommendation(목표 모델)**

```
activeInterval = customIntervalDays ?? defaultIntervalDays
elapsedRatio = (now - lastCleanedAt) / activeInterval
```

**현재 프로토타입 저장소(목표 모델과의 매핑)**

```
localStorage["bbodeuk.setup.v1"]              // 선택한 공간 목록 + 방 이름
localStorage["bbodeuk.preview.points.v2"]     // 시연용 보유 포인트(기본 500P)
sessionStorage["bbodeuk.session.homeCompleted.v1"]      // 오늘 세션에서 완료 처리한 추천 id 목록
sessionStorage["bbodeuk.session.deepClean.recent.v1"]   // 이번 대청소 세션에서 체크한 항목 목록
```

위 4개 키는 서로 다른 화면 각각을 위한 임시 저장소이며, 목표 모델의 `CleaningLog` 테이블 하나로 통합되어야 한다. 추천/상태/Fade는 목표 모델에서는 같은 elapsedRatio를 공유하지만 현재는 화면별로 분리되어 있다.

**(신규 검토) AI 온보딩 관련 데이터:** 아이템별 관계유형(관계없음/광고/제휴) 필드는 SECTION 22의 로드맵이 실행될 때 아이템-가이드 매핑 테이블에 추가할 필드로 남겨둔다. 지금 단계에서 스키마에 반영할 필요는 없다.

## 26. 알림 전략

원칙은 경고가 아니라 부드러운 제안이다. 금지 표현: `청소가 밀렸어요` / `3일 지났어요` / `오늘 꼭 해야 해요`.

**구현 갭:** 헤더의 알림 아이콘은 표시만 하며 실제 알림을 발생시키지 않는다. 설정 화면의 추천/알림 스위치도 현재는 시각적 토글일 뿐 On/Off 상태를 저장하거나 알림 발송에 반영하지 않는다. P1 범위에서 실제 추천 알림 로직과 함께 구현한다.

## 27. 핵심 KPI

**North Star:** Weekly Active Care Users

**Activation:** First Paint Rate / First Paint Time / Empty Home → First Paint / 로그인 진입 → 게스트 전환율 vs 회원가입 완료율 / Setup 완료율(Login/SignUp → 기본 공간 선택 완료)

**Engagement:** Weekly Painted Items / 1-Item 기록 비율 / Quick Record 사용률 / Deep Clean 사용률 / Deep Clean 평균 기록 Item 수 / Additional Paint Rate

**Recommendation:** 추천 Item 노출 → 기록 전환율 / Needs Care → Repaint Conversion / Custom Interval 설정률(P0.5 이후 측정 가능)

**Monetization:** 공간 확장 구매 전환율(기본 공간만 사용 → 300P 확장) / 시작 포인트 300P 소진율 / 포인트 충전 전환율·평균 충전 금액 / 잔액 부족 → 충전 화면 도달 → 충전 완료 퍼널

**Retention:** W1/W4 Retention / 장기 미사용 후 Return

**(신규) AI 온보딩(SECTION 19 도입 시):** 사진 온보딩 경로 선택률(빠른 시작 대비) / AI 인식 결과 수정률 / 사진 경로 이용자의 Setup 완료율·First Paint 도달 시간

## 28. 핵심 리스크 & 대응

1. **추천이 새로운 Todo처럼 느껴질 수 있다** → overdue 언어 금지, 추천 개수 제한, "슬슬 볼 때" 표현
2. **Deep Clean이 체크리스트 앱처럼 보일 수 있다** → 전체 완료·미완료·% 없음
3. **기본 권장 주기가 사용자 집 환경과 맞지 않을 수 있다** → Custom Interval, 기본값은 참고값임을 명시
4. **Item Library가 너무 커져 복잡해질 수 있다** → Core 우선 노출, Hidden은 Discovery
5. **Fade와 추천이 압박으로 느껴질 수 있다** → 숫자 숨김, 색 완전 제거 금지, 알림 제어권 제공
6. **로그인 장벽이 첫 성취를 늘일 수 있다** → 게스트 경로의 시각적 위계를 정기적으로 점검
7. **포인트가 "결제해야 쓰는 앱"이라는 인상을 줄 수 있다** → 포인트/가격 UI는 주 CTA보다 낮은 위계 유지, 청소 기록 동선에는 가격 정보 미노출
8. **화면별 목업 데이터 분절로 실제 지표 측정이 불가능하다**(내부 리스크) → P0.5의 CleaningLog 통합을 최우선 과제로 고정
9. **(신규) AI 인식 오탐지 시 수정 마찰이 크면 신뢰가 깨진다** → 편집 가능한 초안 UX를 정확도보다 우선 설계, 수동 폴백 경로 상시 제공
10. **(신규) AI 온보딩 단계 증가가 First Paint 목표(60초 이내)와 충돌할 수 있다** → "빠르게 시작하기" 경로를 항상 병행 제공
11. **(신규) 아이템 슬롯 유료화가 "핵심 기록은 포인트로 막지 않는다"는 원칙과 충돌할 수 있다** → 도입 전 별도 정책 검증, 한도를 여유 있게 설정

## 29. 제품 설계 원칙

1. 한 Item도 성공이다.
2. 기록은 행동보다 가벼워야 한다.
3. 확인과 기록의 목적을 분리한다.
4. 하지 않은 것을 벌하지 않는다.
5. 상태는 연속적이지만 표현은 단순하게 한다.
6. 청결도를 평가하지 않는다.
7. 작은 성취를 지우지 않는다.
8. 세부 정보가 기본 행동을 방해하지 않는다.
9. Session을 사용자가 관리하지 않는다.
10. 추천은 명령이 아니라 제안이다.
11. 기본값은 앱이 제공하고 사용자는 필요할 때만 조정한다.
12. Deep Clean에서도 전체 완료를 요구하지 않는다.
13. CleaningLog는 덮어쓰지 않고 누적한다.
14. 추천/상태/Fade는 하나의 경과율 모델을 공유한다.
15. 포인트는 청소 행동이 아니라 공간 확장에만 관여한다. 청소를 기록해도 포인트를 얻지 않고, 포인트가 없어도 기본 공간의 청소 기록은 막히지 않는다.
16. 계정 여부는 첫 기록 경험의 단계 수를 바꾸지 않는다. 로그인/회원가입/게스트 중 무엇을 고르든 Setup → Empty Home → First Paint까지의 단계 수는 같아야 한다.
17. **(신규) AI 인식 결과는 확정이 아니라 항상 편집 가능한 초안이다.** 자동으로 저장·확정하지 않는다.
18. **(신규) 청소 방법 가이드는 안전이 우선이다.** AI가 매번 새로 생성하지 않고, 사람이 검수한 정적 콘텐츠로 제공한다.
19. **(신규) AI 온보딩 경로는 선택 사항이다.** 사진 없이도 First Paint까지의 단계 수와 속도가 늘어나지 않아야 한다.

## 30. 장기 제품 방향

Cleaning Tracker에서 Personal Home-care System으로.

STEP 1 기록한다 → STEP 2 기억한다 → STEP 3 상태를 보여준다 → STEP 4 추천한다 → STEP 5 놓친 관리 항목을 발견한다 → STEP 6 사용자 집에 맞게 주기를 조정한다 → STEP 7 쌓인 기록을 해석해 더 적은 판단으로 집을 관리하게 한다

**Product Vision:** 사용자가 집 관리 방법을 모두 기억하고 계획하지 않아도 집을 꾸준히 돌볼 수 있도록 돕는다.

## 31. Validation Plan

01 Recommended Care 문구 압박감 / 02 Ratio Threshold / 03 Default vs Custom Interval 신뢰도 / 04 Deep Clean Accordion 유용성 / 05 Deep Clean 무압박 가치 / 06 Home 우선순위 선호

07 **Login/Guest 분기** — Login 화면에서 게스트 경로 발견 시간, "로그인 없이 둘러보기" 클릭률, 카카오/구글 버튼 대비 게스트 선택 비율

08 **공간 확장 가격 민감도** — "공간 1개 = 300P"라는 가격 단위가 사용자 기대와 맞는지, 시작 포인트 300P를 실제로 첫 확장에 소진하는 비율

09 **(신규) AI 사진 온보딩 신뢰도** — 사진 인식 결과를 그대로 진행하는 비율 vs 수정하는 비율, 수정 시 소요 시간과 이탈 여부, 애매한 공간(복합 공간) 판정 시 사용자 반응

## 32. 용어 정의

| 개념 | 공식 명칭 | 예 |
| --- | --- | --- |
| 관리하는 집 | Home | 우리 집 |
| 큰 관리 영역 | Space | 욕실, 주방 |
| 실제 기록 대상 | Item | 세면대, 변기 |
| 세부 관리 안내 | Care Action | 수전 물때 제거 |
| 집 상태 시각화 | Home Canvas | 공간별 커버리지 캔버스 |
| 관리 경과 | Home Care Status | Needs Care |
| 기록 | CleaningLog | 세면대 9/3 |
| 빠른 개별 기록 | Single Paint | 세면대 카드 탭 |
| 여러 항목 기록 | Batch Paint | 4개 기록 |
| 대규모 청소 | Deep Clean Mode | 주말 집 전체 청소 |
| 기본 권장 주기 | Default Interval | 세면대 7일 |
| 사용자 설정 주기 | Custom Interval | 세면대 10일 |
| 경과 비율 | Elapsed Ratio | 경과일 / 주기 |
| 추천 시스템 | Recommended Care Engine | 다음 관리 대상 추천 |
| 결제 재화 | Point | 공간 확장 300P |
| 유료 확장 단위 | Space Expansion | 공간 1개 추가 |
| 가입 혜택 | Signup Bonus | 시작 포인트 300P |
| AI 공간 인식 온보딩(신규) | AX Onboarding | 사진으로 주방 인식 후 아이템 초안 구성 |

## 33. Appendix — 구현 현황 갭 요약

| 영역 | 정의(원칙) | 현재 구현 | 상태 |
| --- | --- | --- | --- |
| 인증/온보딩 진입 | Login(루트)+SignUp+게스트 | UI만 존재, 백엔드 미연동 | 🆕 신규 결정, 백엔드 미연동 |
| Setup 뒤로가기 경로 | `/login`으로 통일 | 반영 완료 | ✅ 정합 |
| URL 라우팅 방식 | `BrowserRouter`(`/login`) + GitHub Pages `404.html` SPA 폴백 | 반영 완료 | ✅ 정합 |
| 수익 모델 | 포인트 기반 공간 확장 구매 | 결제 미연동 | 🔁 방향 전환, 결제 미연동 |
| Home 상태 | 4상태(Needs Care 포함) | 반영 완료 | 🔁 확장 |
| Bottom Nav 5번째 탭 명칭 | 청소가이드 | 반영 완료 | ✏️ 명칭 정정 |
| Recommended Care Engine | 단일 elapsedRatio 계산 | 화면별 고정 목업값 | ⚠️ 미구현(Phase 1) |
| Home Canvas Fade | 시간 경과 기반 연속 Fade | 관리 항목 수 비율 기반 4단계 채도 | ⚠️ 다른 방식으로 구현, 통합 필요 |
| CleaningLog | 영구 누적 저장 | 브라우저 탭 세션 한정 목업 | ⚠️ 미구현(P0.5) |
| Custom Interval / Custom Item | 사용자가 설정·추가 | 입력 UI만 존재, 저장 안 됨 | ⚠️ 미구현(P0.5) |
| Deep Clean 흐름·종료 정책 | 전체 완료 미요구, 단일 종료 버튼 | 거의 동일하게 구현 | ✅ 정합 |
| 알림 전략 | 부드러운 제안형 알림 | 아이콘/토글 표시만, 발송 없음 | ⚠️ 미구현(P1) |
| Item Library Core/Hidden 구분 | Core 우선, Hidden은 Discovery | 청소가이드 탭에 구현 | ✅ 정합 |
| 뱃지/헤더 UI 원칙 | 알약형 뱃지 최소화, 브레드크럼 아님 | 공간목록·가이드·공간상세는 알약형(원칙과 다름) | 🆕 신규(디자인 시스템 레벨), 일부 ⚠️ |
| **(신규) AI 공간/아이템 인식 온보딩** | 편집 가능한 초안, taxonomy 제한 분류 | 미구현 — 설계만 확정 | 🆕 신규 결정, 구현 전 |
| **(신규) 아이템별 정적 청소가이드 라이브러리** | 안전 검수된 정적 콘텐츠 매칭 | 미구현 | 🆕 신규 결정, 구현 전 |
| **(신규) 준비물 추천/제휴 링크** | SECTION 22 로드맵 | 미구현, 범위 밖 | 🆕 로드맵만 존재 |

**범례:** ✅ 정합 · 🔁 방향 전환 · 🆕 신규 결정 · ✏️ 명칭만 정정 · ⚠️ 아직 미구현/부분 구현
